// Global registration toggle
// Set to true to enable the full registration flow (Terms → Role → Google Form)
// Set to false to show the "Coming Soon" popup instead
const REGISTRATION_OPEN = false;

export default REGISTRATION_OPEN;
